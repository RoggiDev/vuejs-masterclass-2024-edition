import { defaultConfig } from '@formkit/vue'

export default defaultConfig({
  config: {
    classes: {
      outer: 'mb-3',
      label: 'form-label',
      input: 'form-control',
      help: 'form-text',
      messages: 'text-danger',
    },
  },
})
